```xml
<?xml version="1.0" encoding="utf-8"?>
<bitmap
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@mipmap/ic_launcher_round" />
```

**Note:** The `mipmap-anydpi/` directory should contain fallback bitmap references for devices below API 26, while `mipmap-anydpi-v26/` contains the adaptive icons for API 26+ devices.

This fix ensures:
1. Adaptive icons are only used on API 26+ devices
2. Fallback icons are provided for compatibility
3. The build error is resolved while maintaining backward compatibility